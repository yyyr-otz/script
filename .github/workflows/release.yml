name: 更新release文件
run-name: ${{ github.actor }} 正在更新release文件

on:
  schedule:
    - cron: '0 0 * * *'  # 每天UTC时间0点运行
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4.1.1
      with:
        fetch-depth: 0

    - name: Setup environment
      run: |
        sudo apt-get update
        sudo apt-get install -y curl unzip jq

    - name: Get versions
      id: get_versions
      run: |
        # 定义获取版本的函数
        get_latest_version() {
        repo=$1
        curl -sL "https://api.github.com/repos/$repo/releases/latest" | jq -r '.tag_name'
        }
    
        get_prerelease_version() {
        repo=$1
        # 获取所有版本，筛选出prerelease版本，并选择最新的一个
        curl -sL "https://api.github.com/repos/$repo/releases" | \
            jq -r '[.[] | select(.prerelease == true) | .tag_name] | sort_by(split(".") | map(tonumber)) | reverse | first'
        }
    
        compare_versions() {
        # 比较两个版本，返回较大的版本号
        local v1=$1
        local v2=$2
        # 使用sort命令进行版本比较，假设版本号遵循语义化版本格式
        printf "%s\n%s" "$v1" "$v2" | sort -V | tail -n1
        }
    
        # 获取各项目版本
        declare -A versions
    
        # Nezha Agent
        agent_latest=$(get_latest_version nezhahq/agent)
        agent_prerelease=$(get_prerelease_version nezhahq/agent)
        if [ "$agent_prerelease" != "null" ]; then
        agent_version=$(compare_versions "$agent_latest" "$agent_prerelease")
        else
        agent_version=$agent_latest
        fi
        echo "agent_version=${agent_version}" >> $GITHUB_OUTPUT
    
        # Xray Core
        xray_latest=$(get_latest_version XTLS/Xray-core)
        xray_prerelease=$(get_prerelease_version XTLS/Xray-core)
        if [ "$xray_prerelease" != "null" ]; then
        xray_version=$(compare_versions "$xray_latest" "$xray_prerelease")
        else
        xray_version=$xray_latest
        fi
        echo "xray_version=${xray_version}" >> $GITHUB_OUTPUT
    
        # Cloudflared
        cloudflared_latest=$(get_latest_version cloudflare/cloudflared)
        cloudflared_prerelease=$(get_prerelease_version cloudflare/cloudflared)
        if [ "$cloudflared_prerelease" != "null" ]; then
        cloudflared_version=$(compare_versions "$cloudflared_latest" "$cloudflared_prerelease")
        else
        cloudflared_version=$cloudflared_latest
        fi
        echo "cloudflared_version=${cloudflared_version}" >> $GITHUB_OUTPUT

    - name: Process nezha-agent
      run: |
        # 定义所有agent平台配置
        declare -A agent_platforms=(
          ["linux-arm64"]="nezha-agent_linux_arm64.zip"
          ["linux-amd64"]="nezha-agent_linux_amd64.zip"
          ["freebsd-arm64"]="nezha-agent_freebsd_arm64.zip"
          ["freebsd-amd64"]="nezha-agent_freebsd_amd64.zip"
        )

        mkdir -p agent-bin
        VERSION=${{ steps.get_versions.outputs.agent_version }}

        for tag in "${!agent_platforms[@]}"; do
          filename=${agent_platforms[$tag]}
          dir="$tag"  # 保留原始tag作为目录名

          echo "处理 $tag => $filename"
          curl -sL "https://github.com/nezhahq/agent/releases/download/$VERSION/$filename" -o "${filename}"
          mkdir -p "agent-bin/${dir}"
          unzip -j "${filename}" 'nezha-agent' -d "agent-bin/${dir}"
          mv "agent-bin/${dir}/nezha-agent" "agent-bin/${dir}/nezha-agent-v1"
          chmod +x "agent-bin/${dir}/nezha-agent-v1"
          rm "${filename}"
        done

    - name: Process Xray
      run: |
        # 定义所有Xray平台配置
        declare -A xray_platforms=(
          ["linux-arm64"]="Xray-linux-arm64-v8a.zip"
          ["linux-amd64"]="Xray-linux-64.zip"
          ["freebsd-arm64"]="Xray-freebsd-arm64-v8a.zip"
          ["freebsd-amd64"]="Xray-freebsd-64.zip"
        )

        mkdir -p xray-bin
        VERSION=${{ steps.get_versions.outputs.xray_version }}

        for tag in "${!xray_platforms[@]}"; do
          filename=${xray_platforms[$tag]}
          dir="$tag"  # 保留原始tag作为目录名

          echo "处理 $tag => $filename"
          curl -sL "https://github.com/XTLS/Xray-core/releases/download/$VERSION/$filename" -o "${filename}"
          mkdir -p "xray-bin/${dir}"
          unzip -j "${filename}" 'xray' -d "xray-bin/${dir}"
          chmod +x "xray-bin/${dir}/xray"
          rm "${filename}"
        done

    - name: Process Cloudflared
      run: |
        # Cloudflared平台配置
        declare -A cloudflared_platforms=(
          ["arm64"]="cloudflared-linux-arm64"
          ["amd64"]="cloudflared-linux-amd64"
        )

        VERSION=${{ steps.get_versions.outputs.cloudflared_version }}

        for tag in "${!cloudflared_platforms[@]}"; do
          mkdir -p cloudflared-bin/$tag
          filename=${cloudflared_platforms[$tag]}
          echo "处理 $tag => $filename"
          curl -sL "https://github.com/cloudflare/cloudflared/releases/download/$VERSION/$filename" -o "cloudflared-bin/$tag/argo"
          chmod +x "cloudflared-bin/$tag/argo"
        done

    - name: Update releases
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        # nezha-agent发布配置
        declare -A agent_tags=(
          ["linux-arm64"]="arm64"
          ["linux-amd64"]="amd64"
          ["freebsd-arm64"]="freebsd-arm64"
          ["freebsd-amd64"]="freebsd-amd64"
        )

        for src_tag in "${!agent_tags[@]}"; do
          target_tag=${agent_tags[$src_tag]}
          gh release upload "$target_tag" "agent-bin/$src_tag/nezha-agent-v1" --clobber
        done
      
        # Xray发布配置
        declare -A xray_tags=(
          ["linux-arm64"]="arm64"
          ["linux-amd64"]="amd64"
          ["freebsd-arm64"]="freebsd-arm64"
          ["freebsd-amd64"]="freebsd-amd64"
        )

        for src_tag in "${!xray_tags[@]}"; do
          target_tag=${xray_tags[$src_tag]}
          gh release upload "$target_tag" "xray-bin/$src_tag/xray" --clobber
        done

        # Cloudflared发布配置
        declare -A cloudflared_tags=(
          ["arm64"]="arm64"
          ["amd64"]="amd64"
        )

        for src_tag in "${!cloudflared_tags[@]}"; do
          target_tag=${cloudflared_tags[$src_tag]}
          gh release upload "$target_tag" "cloudflared-bin/$src_tag/argo" --clobber
        done
