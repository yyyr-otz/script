name: Static Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        arch: [amd64, arm64]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Build static binary
        uses: docker/build-push-action@v4
        with:
          platforms: linux/${{ matrix.arch }}
          context: .
          file: alist-tgbot/Dockerfile
          outputs: type=local,dest=./dist

      - name: Verify build output
        run: |
          echo "构建产物路径结构:"
          ls -R ./dist
          echo "确认二进制文件存在:"
          find ./dist -name alist-tgbot

      - name: Upload to Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # 动态获取二进制文件路径
          BINARY_PATH="./dist/linux_${{ matrix.arch }}/alist-tgbot"
          RELEASE_TAG="${{ matrix.arch }}"
          
          # 确保Release存在，如果不存在则创建
          #if ! gh release list | grep -q "$RELEASE_TAG"; then
          #  gh release create "$RELEASE_TAG" --title "AList-TGBot ($RELEASE_TAG)" --notes "Static build for $RELEASE_TAG"
          #fi
          
          # 上传文件并覆盖同名文件
          gh release upload "$RELEASE_TAG" "$BINARY_PATH" --clobber
